- docker-compose up -d


- docker exec -it airflow airflow users reset-password -u admin --password admin123

- {"admin": "admin123"}

Terminal 1 — Vitals Producer:

bash
cd ~/Real-Time-IoT-Patient-Monitoring-Pipeline && source .venv/bin/activate && python3 scripts/iot_vitals_producer.py --delay 0.5 --loop
Terminal 2 — Movement Producer:

bash
cd ~/Real-Time-IoT-Patient-Monitoring-Pipeline && source .venv/bin/activate && python3 scripts/iot_movement_producer.py --delay 0.05 --loop
Terminal 3 — Alert Engine:

bash
cd ~/Real-Time-IoT-Patient-Monitoring-Pipeline && source .venv/bin/activate && python3 scripts/simple_alert_engine.py
Terminal 4 — Kafka → S3 Stream:

bash
cd ~/Real-Time-IoT-Patient-Monitoring-Pipeline && source .venv/bin/activate && python3 scripts/kafka_to_s3_stream.py